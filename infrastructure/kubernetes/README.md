# Kubernetes

Kubernetes manifests will be introduced after the Docker Compose application is stable.

The first target should contain:

- API Deployment and Service
- Web Deployment and Service
- ConfigMap for non-secret configuration
- Secret placeholder for database credentials
- Local cluster instructions using kind
